server:
  port: 8888
spring:
  application:
    name: erudio-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/msmaker/erudio-config-server
          #username: 
          #password: 
          default-label: main
          search-paths:
          - 'greeting-service*'
          